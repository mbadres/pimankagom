import 'package:flutter/material.dart';

final ThemeData basicLightTheme = ThemeData(
  colorScheme: ColorScheme.fromSeed(seedColor: Colors.amber),
);

final ThemeData basicDarkTheme = ThemeData.dark();
